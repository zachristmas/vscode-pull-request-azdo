# v1.5 UX Modernization, Phase B: next-session kickoff prompt

Paste the block below to start the Phase B session. It assumes Phase A and the
live-feedback fixes are already committed on `fix/v1.4-repair` (see the design
doc `v1.5-ux-modernization.md` Section 7 "Session status" block for the full
state and commit refs).

---

Implementation session for v1.5 UX modernization, Phase B. Continue on branch
`fix/v1.4-repair` in `~/src/vscode-pull-request-azdo`.

Read `docs/fork/designs/v1.5-ux-modernization.md` in full, especially the Section 7
"Session status" block (records what Phase A plus partial Phase B already landed) and
Sections 3 (UX-01 Your review card), 2.3 (UX-02 outcome card), 4.2.2 (thread
container), 4.2.3/4.2.5 (timeline and description). Also read `ROADMAP.md` Section 5
(per-item workflow, routing, session hygiene) and Section 2.9 (UX-\* entries, now
annotated with Phase A/B status).

State: Phase A items 1 to 6 are committed (`ce9142bc`, `17783b20`, `f0e18f8c`,
`edfff301`, `da2403c9`, `8c166ca1`). Item 9's core (single-card comment restyle),
colored status icons, reply left-align, and page bottom margin landed early from live
feedback (`1380eaf5`). A functional tree fix (merged PRs with a deleted source branch
now show files) landed as `ba7f645f`. Re-verify every current-state file:line in the
design doc before editing: both the policy/auto-complete work and this session's UX
commits have shifted these files, so cite against HEAD, not the doc's line numbers.

Implement Phase B, one commit per item, in Section 7 order:

7. [UX-01] "Your review" card (Section 3.2): replace VotePanel in `sidebar.tsx` with a
   card. Always-visible current-vote row (icon plus VOTE_STATE_TEXT); controlled select
   of the four real votes (drop the "(current vote)" option hack; move Reset Vote to a
   link shown only when vote is not 0); three-phase cast feedback (busy "Casting..."
   plus aria-busy; success transient "Vote recorded" in `--azdo-success` wrapped in
   `aria-live`; failure caught and shown as "Vote failed, try again" in `--azdo-danger`).
   Export REVIEW_STATE_ICON / VOTE_STATE_TEXT from `reviewer.tsx` instead of
   duplicating. Section-header treatment (11px uppercase muted); empty categories render
   "None". Reviewer-row flex fix: `common.css` `.section-item .login` width:129px
   becomes flex:1; min-width:0 with ellipsis. Verify live: cast, change, reset, and fail
   (disconnect network) all give visible feedback.

8. [UX-02] Outcome summary card (Section 2.3): on finished PRs, the space where
   VotePanel lived becomes an OutcomeSummary card in `sidebar.tsx`: status dot plus bold
   label (Merged is success, Abandoned is muted), reuse `getClosedCommentDescription`
   (`header.tsx`) for "N/M threads resolved", and "Your vote: [icon] X" only if the
   current user voted. Card styling: `--azdo-surface`, `--azdo-border-subtle`,
   `--azdo-radius-lg`, `--azdo-space-3` padding. Do after 7 (shares the card plus
   vote-row rendering). This is the deferred half of UX-02; isActive gating already
   removed the controls cleanly.

9. [UX-03] Thread container plus status pill plus collapse plus ghost reply
   (Section 4.2.2), the largest item. Thread owns the border/card with a 2px left rail
   for replies. Move the thread status control from the first comment's header to the
   thread header (render it in CommentEventView, which already owns thread.status /
   onThreadStatusChange; delete the fragile isFirstCommentInThread / c.id === 1
   computation in `timeline.tsx` and the statusProps plumbing in `comment.tsx`). Status
   is a colored pill (Active is info, Pending is warning, Fixed/Closed is success,
   WontFix is muted). Resolved threads (Fixed/WontFix/Closed) collapse by default to a
   one-line muted summary plus chevron (per-render useState, no persistence). Replace
   the right-aligned Reply button with a full-width ghost "Reply..." field that swaps to
   the real ReplyToThread composer on click. This is what fixes the "which thread am I
   replying to" ambiguity flagged in review. DESIGN DECISION to make live: commit
   `1380eaf5` gave each comment its own card, but 4.2.2's model is the thread owning the
   card with inner comments losing their borders. Reconcile these (likely: thread card
   with borderless, rail-separated inner comments) rather than nesting card-in-card.
   Verify live: create, reply, resolve, reopen, collapse-expand on a 3-plus comment
   thread, and confirm a file-anchored thread's diff-header chip still opens the diff.

10. [UX-03] Description placeholder plus timeline polish (Section 4.2.3 / 4.2.5): empty
    pr.body renders "No description provided" in a dashed-border `--azdo-muted` card (an
    affordance that it is fillable). System/commit timeline events become muted
    single-line rows, 16px icon, right-aligned timestamp (mostly inherit the spacing
    scale). Keep newest-first ordering and composer-above-timeline; record as
    deliberate, do not "fix".

Verify each item per Section 5.1 before committing: `npx tsc --noEmit -p tsconfig.json`,
`yarn run bundle`, then F5 smoke against a real PR in policy-verify. This is visual
work: eyeball each change in light and dark plus one high-contrast spot check, and drop
before/after screenshots into the PR. One item is one commit referencing the UX-ID;
strike or annotate the ROADMAP Section 2.9 UX-0x entry in the same commit (UX-01 done
after 7; UX-02 done after 8; UX-03 done after 10 plus 11). Commit per item; do not push.

Carry-over check first: confirm the tree fix (`ba7f645f`) actually renders files and
opens diffs on a merged PR whose source branch was deleted, and that tree-checkout on
such a PR still behaves (it now has a resolved head ref pointing at a gone branch).

Out of scope (recorded in Section 7 "Follow-on"): composer formatting toolbar (do with
DLA-07), "View merge commit" link (needs lastMergeCommit in the payload), editor-margin
thread styling (THR-01/02, v1.7). Also still out: PR-list pagination and the
strict/strictNullChecks migration.
