# v1.5 UX Modernization, Phase C: live-verify, iterate, ship (next-session kickoff prompt)

Paste the block below to start the session. It assumes Phase A and Phase B are
code-complete on `fix/v1.4-repair` (see the design doc `v1.5-ux-modernization.md`
Section 7 "Session status" block for the full state and commit refs). Phase B was
`tsc`/`bundle`/lint-clean but the visual commits are first passes; this session
turns your live F5 pass into fixes and then closes out the v1.5 UX milestone.

Before pasting: do your F5 pass first and collect findings. Where indicated below,
paste your punch-list (what looks or behaves wrong, in which theme). If you found
nothing, say so and the session goes straight to close-out.

---

Session for v1.5 UX modernization, Phase C (live-verify, then iterate, then ship).
Continue on branch `fix/v1.4-repair` in `~/src/vscode-pull-request-azdo`.

Read `docs/fork/designs/v1.5-ux-modernization.md` in full, especially Section 7
"Session status" (records the Phase B commits) and the design of the surfaces you
are verifying: Section 3 (UX-01 Your-review card), 2.3 (UX-02 outcome card), 4.2.1
(comment card), 4.2.2 (thread container, status pill, collapse, ghost reply),
4.2.3 and 4.2.5 (timeline and description). Also read `ROADMAP.md` Section 5
(per-item workflow, routing, session hygiene) and Section 2.9 (UX-\* now all
struck). Re-verify every current-state file:line against HEAD before editing:
Phase B shifted these files heavily, so cite HEAD, not the doc's line numbers.

Phase B commits, for reference: `e5f2d7dc` (UX-01), `b210713c` (lint), `c3fcc82f`
(UX-02), `8b74d525` (UX-03 thread), `d5cb2f87` (UX-03 reopen fix), `56191131`
(UX-03 description and timeline).

## 1. Apply my live punch-list

I ran F5 against a real PR in `policy-verify` (light plus dark plus one
high-contrast spot check). Findings to fix, most impactful first:

<PASTE PUNCH-LIST HERE. e.g. "dark: status pill chevron invisible", "light: muted
timeline rows too faint", "reply-ghost does not fill the card width", etc. If
empty, write "none, proceed to close-out".>

For each fix: smallest change that resolves it, keep the token system (no raw
hex/rgba outside the `.vscode-high-contrast` blocks), one commit per coherent fix
referencing the UX-ID. `tsc --noEmit`, `yarn run bundle`, and eslint on touched
files before committing. Do not push.

## 2. Proactive self-review of the unverified-by-agent spots

These were built without live eyes and are the likeliest to need a nudge even if
my punch-list misses them. Check each in light plus dark; fix if wrong, otherwise
leave and say so. Do not churn working CSS:

- Status pill as a native `<select>` (`.thread-status-pill` in `index.css`): the
  borderless transparent select must still show its chevron and be legible on both
  themes; the colored dot plus pill border must read against `--azdo-surface`.
- Reply-ghost sizing (`.reply-ghost`): `display:block` plus margins must fill the
  thread card width, not shrink to content.
- Empty-description card-in-card: the dashed `.description-placeholder` sits inside
  the description's own comment card. Confirm that reads as intentional, not a
  double box.
- Muted timeline rows in light theme: `--azdo-muted` on `.commit`/`.merged` rows
  must not drop below readable contrast (WCAG-ish eyeball).
- Cast feedback timing and announcement: the 2.5s success transient and the
  `aria-live` announcement. Confirm the success span replacing the button does not
  cause a layout jump, and that a failed cast leaves the button retryable.
- Thread reply rail alignment: the 2px left rail plus top separator on replies
  should line up cleanly across a 3-plus comment thread.

## 3. Close out the v1.5 UX milestone

Once sections 1 and 2 are clear and I confirm the visuals are good (ask me if
unsure; do not ship on your own judgment of unseen visuals):

- Update `CHANGELOG.md` under the pending version heading with the UX-01..05 lines
  (one concise bullet per UX-ID; this is the milestone, not per-commit).
- Version bump per Section 5.4 (v1.5.0 cadence: bump plus CHANGELOG plus package
  once per completed milestone).
- `npx @vscode/vsce package --no-dependencies`, install the `.vsix` locally, and
  smoke it once more.
- If a PR is wanted for this fork, open it as a draft with before/after
  screenshots; no local or personal filesystem paths in the description.
  Otherwise follow Section 5.4's "tag v1.x.0 on master" once merged. Confirm the
  git flow with me before merging or tagging.

## Out of scope (recorded in Section 7 "Follow-on"; do not start here)

Composer formatting toolbar (do with DLA-07), "View merge commit" link (needs
`lastMergeCommit` in the webview payload), editor-margin thread styling (THR-01 /
THR-02, v1.7). Also still out: PR-list pagination and the `strict` /
`strictNullChecks` migration. The three pre-existing `timeline.tsx` warnings live
in the commented-out `ReviewEventView` scaffolding; leave them unless you are
separately cleaning that dead code.
